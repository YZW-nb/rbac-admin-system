<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.admin.mapper.SysDictDataMapper">

    <select id="selectDictDataByType" resultType="com.admin.entity.SysDictData">
        SELECT id, dict_type, dict_label, dict_value, sort, css_class, status, remark, create_by, create_time
        FROM sys_dict_data
        WHERE dict_type = #{dictType}
          AND status = 1
        ORDER BY sort
    </select>

</mapper>
